<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">

        <!-- MainActivity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.RegistrationActivity"
            android:exported="false" />

        <activity
            android:name=".activities.AdminActivity"
            android:exported="false" />

        <activity
            android:name=".activities.TeacherActivity"
            android:exported="false" />

        <activity
            android:name=".activities.StudentActivity"
            android:exported="false" />

        <activity
            android:name=".activities.TeacherActionsActivity"
            android:exported="false" />

        <activity
            android:name=".activities.GroupActionsActivity"
            android:exported="false" />

        <activity
            android:name=".activities.StudentActionsActivity"
            android:exported="false" />
    </application>

</manifest>